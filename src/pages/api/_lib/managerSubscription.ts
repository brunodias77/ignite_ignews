export async function saveSubscription(
  subscriptionId: string,
  custometId: string
) {
  //buscar o usuario no banco do fauna com o id (custumerID)
  // salvar os dados sa subscription do usuario no faunaDb
}
